<h1>Add a comment</h1>

<%= render 'shared/error_messages' %>

<form action="<%= goal_comments_url %>" method="post">
    <%= insert_form_authenticity_token %>

    <input
        type="hidden"
        name="goal_comment[subject_id]"
        value="<%= params[:goal_id] %>">

    <label for="comment_text">Enter your comment:</label>
    <textarea name="goal_comment[comment_text]" id="comment_text" cols="30" rows="10"></textarea>

    <input type="submit" value="Submit comment">
</form>
