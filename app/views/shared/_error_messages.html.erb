<% if flash[:errors] %>
    <div class="error-messages">
        <p>Please fix the following issue(s):</p>
        <ul>
            <% flash[:errors].each do |error| %>
                <li><%= error %></li>
            <% end %>
        </ul>
    </div>
<% end %>
